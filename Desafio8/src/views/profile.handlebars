<div>
    <h1>Your Profile (sin datos sensibles)</h1>
    <p>{{user.name}}</p>
    <p>{{user.email}}</p>
    <p>{{user.age}}</p>
    <div>
        <p>Cart:</p>{{user.cart}}
    </div>
</div>

<div>
    <a href="/products">Back to Products</a>
</div>